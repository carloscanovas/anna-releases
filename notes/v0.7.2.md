**Sensibilidad táctil ajustable desde el panel**
- feat(server,sdk,panel,kiosk): la pausa anti-rebote del toque (hasta ahora la constante `TAP_REPEAT_MS = 250`) pasa a ser un ajuste por aparato en Dispositivo → Analítica: atajos Normal 250 · Tolerante 400 · Muy tolerante 600 ms, o valor libre entre 100 y 1000. El kiosk lo aplica en caliente (lo consulta al arrancar y cada minuto, por `GET /api/kiosk/touch`, sin sesión). Migración 030 (`device_settings.tap_repeat_ms`, fábrica 250: nada cambia hasta que alguien lo toque).
- feat(panel): el informe de la captura dice a cuántos ms cae el eco del marco (percentil 95 y máximo de las segundas pulsaciones en el mismo sitio en menos de 1 s) y cuántos habría filtrado la pausa vigente, para elegir el valor con una medida.
- Contexto: el anti-rebote llegó en tres pasos (v0.6.3 mismo control + radio, insuficiente; v0.6.4 solo tiempo; v0.6.5 ventana de módulo). Un aparato que siga doblando en ≥ v0.6.5 se ajusta aquí; uno en v0.6.3 lo que necesita es actualizarse.