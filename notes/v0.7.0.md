**Orientación Espacial: aceras, pasos de cebra y trazo que responde al dedo**
- feat(orientation-spatial): el pueblo pasa de un grafo de intersecciones a un grafo de esquinas de manzana: aceras a lo largo de cada calle, doblar la esquina donde hay manzana y pasos de cebra como aristas entre esquinas enfrentadas, sembrados por densidad con conectividad garantizada. Calles de salida por el borde (siempre con cebra) y afueras decorativas que rellenan la vista.
- feat(orientation-spatial): el dedo ya no se detecta con `elementFromPoint` — que dejaba una baldosa muda entre intersecciones y el residente creía que la pantalla no respondía — sino proyectándolo sobre la arista de acera que recorre: el trazo avanza y retrocede en cada fotograma, confirma la esquina al llegar y se pinta por ref, sin renders por muestra. Antirrebote de dos muestras, toque exploratorio sin castigo y un solo dedo por arrastre (`pointerId`).
- feat(orientation-spatial): cruzar sin cebra avisa en niveles 1-3 (densidad 0,7) y bloquea en 4-10 (0,5 / 0,3). Métricas nuevas `error{jaywalk}` y `trace_deviation` (precisión motora del trazo, aún sin lector en el panel). `scoreRoute` resta 6 por cruce indebido y 2 por paso de exceso: un bloque son ahora dos pasos, así que `steps` no es comparable con el histórico.
- feat(orientation-spatial): manzanas y parques con el diseño original más un bordillo oscuro frente a la acera (con losas); tarjetas de hito solo con icono y un pin blanco sobre la esquina de entrada; el nombre del destino sigue en la consigna.
- Migración 029: la instrucción por defecto del juego habla de acera y pasos de cebra, solo si el staff no la había editado.

**Panel: fatiga cognitiva y métricas de profundidad (documento clínico, §7)**
- feat(server,panel): fatiga intra-sesión — la partida se parte en tres tramos por número de respuestas y se comparan mediana del TR, tasa de error y dispersión robusta; veredicto `fatigued`/`stable`/`improving`/`insufficient` (mínimo 12 respuestas) con su curva, en el detalle de cada partida. Las omisiones cuentan como respuesta errónea.
- feat(server,panel): métricas de profundidad por juego — amplitud sostenida (simon), retrocesos con denominador e interferencia (memory-pairs), interferencia por conjunción y patrón de búsqueda (attention-search, object-search; solo para partidas nuevas, que ahora llevan el índice de celda y la similitud del distractor).
- feat(server,panel): percentiles por área cognitiva contra el propio historial (seis meses anteriores, mínimo cuatro meses con dato) y contra la cohorte del mismo GDS por área, solo con n≥5 y etiquetada «local, no normativo», más consistencia entre pruebas.
- feat(server,panel): alertas de fatiga (3 de las últimas 5 partidas valorables) y de estancamiento (4 semanas, 8 partidas), esta última redactada como observación.
- Sin rutas ni migraciones nuevas para el panel: extiende `/performance` y el endpoint de eventos de sesión. «Intentos por nivel» se retiró: los juegos emiten `level_up` una sola vez al acabar.

**Seguridad**
- fix(server,pi): el servidor se niega a arrancar en producción con el `JWT_SECRET` de fábrica; `anna-ensure-secrets.sh` genera uno por dispositivo en `/var/lib/anna/shared/env`.

Pendiente de verificación en la Pi real: todo lo anterior se ha visto en escritorio; la tolerancia del trazo (0,45 baldosas) se calibra contra el marco infrarrojo.