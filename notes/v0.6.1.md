**Por cable también se puede actualizar**
- fix(server): "Buscar actualizaciones" respondía **"sin conexión a internet"** en un dispositivo que tenía internet de sobra. El pre-check llamaba a `anna-network-test-internet.sh`, que ata el `curl` a `wlan0` a propósito; en una Pi conectada por ethernet ese test falla siempre, así que el endpoint devolvía 503 y ni arrancaba el updater. **Por cable no se podía actualizar desde el panel, nunca.** Ahora el pre-check pide el mismo manifest que pedirá el updater, por la ruta por defecto. El test de `wlan0` se queda intacto en la pantalla de Red, que es donde la pregunta "¿tiene salida ESTA WiFi?" es la correcta.